resources:
  - ../../base
  - sealedsecret-jira.yaml

patches:
- patch: |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: alert-jira-gateway
    spec:
      replicas: 2
      template:
        spec:
          containers:
          - name: alert-jira-gateway
            image: alert-jira-gateway:cid